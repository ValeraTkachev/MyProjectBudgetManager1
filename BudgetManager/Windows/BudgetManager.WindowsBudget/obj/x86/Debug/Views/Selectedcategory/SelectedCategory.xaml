<common:ViewBase
    x:Name="pageRoot"
    x:Class="BudgetManager.WindowsBudget.Views.Selectedcategory.SelectedCategory"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BudgetManager.WindowsBudget.Views.Selectedcategory"
    xmlns:common="using:BudgetManager.WindowsBudget.Common"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:i="using:Microsoft.Xaml.Interactivity"
    xmlns:core="using:Microsoft.Xaml.Interactions.Core"
    xmlns:VM="using:BudgetManager.Shared"
    xmlns:Winrt="using:WinRTXamlToolkit.Controls"
    xmlns:mybehaviours="using:BudgetManager.WindowsBudget.Behaviors" 
    xmlns:converter="using:BudgetManager.Core.Converters"
    DataContext="{Binding Source={StaticResource Locator}, Path=SelectedCategoryViewModel}"
    xmlns:controls="using:BudgetManager.WindowsBudget.Views.Controls"
    xmlns:datepicker="using:StoreAppLib.Controls"
    mc:Ignorable="d">

    <Page.Resources>
        <!-- TODO: Delete this line if the key AppName is declared in App.xaml -->
        <x:String x:Key="AppName"></x:String>
        
        <converter:BooleanToVisibilityConverter x:Key="BoolToVisibility"/>
        
    </Page.Resources>
    
    <ScrollViewer HorizontalScrollBarVisibility="Auto" Background="{StaticResource ForAppBarBrush}" VerticalScrollBarVisibility="Disabled">

        <Grid Background="{StaticResource BackgroundBrush}">
        <Grid.ChildrenTransitions>
            <TransitionCollection>
                <EntranceThemeTransition/>
            </TransitionCollection>
        </Grid.ChildrenTransitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="140"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid>
                <controls:MainPanel/>
            
            <ComboBox  x:Name="usernameComboBox"
                       ItemsSource="{Binding Users}" 
                       Style="{StaticResource UsersTransactionCombobox}"
                       VerticalAlignment="Center"
                       HorizontalAlignment="Right" 
                       Margin="0,30,60,0"
                       SelectedItem="{Binding SelectedUser,Mode=TwoWay}"
                       Grid.Column="2">
                
                <i:Interaction.Behaviors>
                    <core:EventTriggerBehavior EventName="SelectionChanged">
                        <core:InvokeCommandAction Command="{Binding SelectionUserChange}"/>
                    </core:EventTriggerBehavior>
                </i:Interaction.Behaviors>
                
               
            </ComboBox>
            
            <StackPanel Orientation="Horizontal" 
                        VerticalAlignment="Center"
                        HorizontalAlignment="Center"
                        Grid.Column="1"
                        Margin="0,35,0,0" 
                        Height="24">
                
                <datepicker:DatePicker Background="{StaticResource ForChartBackGround}" Style="{StaticResource CalendarStyle}" Date="{Binding ThisDate,Mode=TwoWay}">
                    
                    <i:Interaction.Behaviors>
                        <core:EventTriggerBehavior EventName="SelectionChanged">
                            <core:InvokeCommandAction Command="{Binding DateChangeCommand}"/>
                        </core:EventTriggerBehavior>
                    </i:Interaction.Behaviors>
                    
                </datepicker:DatePicker>
                
                <TextBlock Text="-"
                           FontSize="20" 
                           Grid.Column="1" 
                           VerticalAlignment="Center" 
                           HorizontalAlignment="Center"
                           Foreground="{StaticResource ForMainText}"
                           Visibility="{Binding Path = SecondDateVisibility,Mode=TwoWay, Converter={StaticResource BoolToVisibility}}"/>
                
                <datepicker:DatePicker Background="{StaticResource ForChartBackGround}" 
                                       Style="{StaticResource CalendarStyle}" 
                                       Date="{Binding SecondDate,Mode=TwoWay}"
                                       Visibility="{Binding Path = SecondDateVisibility,Mode=TwoWay, 
                    Converter={StaticResource BoolToVisibility}}">
                    
                    <i:Interaction.Behaviors>
                        <core:EventTriggerBehavior EventName="SelectionChanged">
                            <core:InvokeCommandAction Command="{Binding SecondDateChangeCommand}"/>
                        </core:EventTriggerBehavior>
                    </i:Interaction.Behaviors>
                    
                </datepicker:DatePicker>
                
            </StackPanel>
            
        </Grid>
        
        <StackPanel Margin="0,140,0,0"
                    Grid.RowSpan="10">
            
            <StackPanel Orientation="Horizontal" Margin="105,0,0,0">

                <RadioButton x:Uid="daytitle"
                             Command="{Binding DayCommand}" 
                             Style="{StaticResource TextBlockButtonStyle1}"
                             Margin="20,0,0,0"/>

                <RadioButton x:Uid="weektitle"
                             Command="{Binding WeekCommand}" 
                             Style="{StaticResource TextBlockButtonStyle1}"
                             Margin="20,0,0,0"/>

                <RadioButton x:Uid="monthtitle"
                             Command="{Binding MonthCommand}"
                             Style="{StaticResource TextBlockButtonStyle1}"
                             IsChecked="True"
                             Margin="20,0,0,0"/>

                <RadioButton x:Uid="yeartitle"
                             Command="{Binding YearCommand}" 
                             Style="{StaticResource TextBlockButtonStyle1}"
                             Margin="20,0,0,0"/>

                <RadioButton x:Uid="periodtitle"
                             Command="{Binding PeriodCommand}" 
                             Style="{StaticResource TextBlockButtonStyle1}"
                             Margin="20,0,0,0"/>
                
            </StackPanel>
            
            <StackPanel Orientation="Vertical" 
                        Margin="105,5,0,0" 
                        Width="100" 
                        HorizontalAlignment="Left">
                
                <Button Style="{StaticResource AddButtonEllipseStyle}"
                        Command="{Binding AddSubcategoryCommand}" HorizontalAlignment="Center"/>

                <TextBlock x:Uid="addtitle"                         
                           VerticalAlignment="Bottom"
                           HorizontalAlignment="Center"
                           Style="{StaticResource AddTextBlockStyle}"  />
                
            </StackPanel>


                <GridView  x:Name="gvexpense" 
                           Style="{StaticResource expenseGridView}"
                           Grid.Row="2"
                           Margin="105,-10,0,0"
                           ItemsSource="{Binding Path= SelectedUser.Subcategories}"
                           SelectedItem="{Binding SelectedCategory,Mode=TwoWay}">

                    <i:Interaction.Behaviors>
                    <core:EventTriggerBehavior EventName="SelectionChanged">
                        <core:InvokeCommandAction Command="{Binding SelectItem}"/>
                    </core:EventTriggerBehavior>
                    <mybehaviours:GridViewHeightAndWidth/>
                </i:Interaction.Behaviors>
                
               
            </GridView>

            <StackPanel Orientation="Horizontal"
                        HorizontalAlignment="Right"
                        Margin="0,-100,20,0" >

                <Button Content="круги" 
                        x:Uid="circles" 
                        Style="{StaticResource PressedStyle1}" />

                <Button Content="столбцы"
                        x:Uid="columns"
                        Style="{StaticResource UnpressedStyle1}">
                    
                    <i:Interaction.Behaviors>
                        <core:EventTriggerBehavior EventName="Click">
                            <core:NavigateToPageAction TargetPage="BudgetManager.WindowsBudget.Views.Selectedcategory.SelectedCategoriesLinear"/>
                        </core:EventTriggerBehavior>
                    </i:Interaction.Behaviors>
                    
                </Button>
                
            </StackPanel>

        </StackPanel>

    </Grid>
        
    </ScrollViewer>
        
    <Page.BottomAppBar>
        
        <AppBar x:Name="bottomAppBar"
                Padding="10,0,10,0" 
                Background="{StaticResource ForAppBarBrush}">
            
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>
                
                <StackPanel Orientation="Horizontal">
                    
                    <AppBarButton Name="editexpense" 
                                  x:Uid="editexpense"
                                  Icon="Edit"
                                  Command="{Binding EditSubcategoryCommand}"
                                  Visibility="{Binding Unchosen,Mode=TwoWay, Converter={StaticResource BoolToVisibility}}"/>

                    <AppBarButton Name="deleteexpense"
                                  x:Uid="delete"
                                  Command="{Binding DeleteCommand}" 
                                  Visibility="{Binding Unchosen,Mode=TwoWay, Converter={StaticResource BoolToVisibility}}">
                        
                        <AppBarButton.Icon>
                            
                            <FontIcon Glyph="&#xe106;" 
                                      FontFamily="Segoe UI Symbol" 
                                      Margin="0,2,0,0"/>
                            
                        </AppBarButton.Icon>
                        
                    </AppBarButton>
                    
                </StackPanel>
                
                <StackPanel Grid.Column="1"
                            HorizontalAlignment="Right" 
                            Orientation="Horizontal">
                    
                    <AppBarButton Name="addexpense"
                                  x:Uid="addexpense"
                                  Style="{StaticResource AddExpenseButtonStyle}" 
                                  Command="{Binding AddSubcategoryCommand}"/>
                    
                </StackPanel>
                
            </Grid>
            
        </AppBar>

    </Page.BottomAppBar>
    
</common:ViewBase>

